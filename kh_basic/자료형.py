# 문자열, 숫자 (정수/실수), Boolean (t/f)
# 변수 => 자료형을 이용해서 실제 데이터를 저장할 공간을 할당 받는 것.
# 변수에 값을 대입하는 연산자 " = "

a = b = c = 1
a, b, c = 1, True, "Aghanim" # 파이썬에서는 이런식으로 한반에 대입 가능.

# 변수의 타입 확인: type()
print(type(a))
print(type(b))
print(type(c))

